# _Worker_Controller : _CLI_Controller

`_Worker_Controller` is a subclass of `_CLI_Controller` to execute a console program in a worker. 

The class is a template for implementing callback functions.